  # NOTE
  On running the project on your localhost no login or password is required you can directly click on the login button and you will redirected to the dashboard

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  